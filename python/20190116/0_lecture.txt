1 正则表达式
  1.1 字符集(匹配单一字符)([a-zA-Z0-9_])
  1.2 概括字符集(\d, \D, \w, \W, \s, \S, .)
  1.3 字符定界(a[bd]c, a[^bd]c)
  1.4 数量词({2}, {2,4})
  1.5 概括数量词(*, +, ?)
  1.6 字符组
  1.7 正则替换和正则切分(re.sub, re.split)
  1.8 正则匹配(re.match, re.search)
  1.9 group分组
  1.10 正则预编译
  1.11 正则表达式使用技巧

2 python原生爬虫
  需求: 创建一个爬虫类，爬取直播网站中每个分类的主播信息并按照人气排序打印。
  